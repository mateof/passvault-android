<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only. This file is in src/debug, so the published application never sees it and
  keeps Android's default: cleartext refused.

  Development needs it because a PassVault server on a laptop is plain HTTP, and 10.0.2.2 is the
  host as the emulator sees it. Allowing cleartext everywhere would be the easy version and the
  wrong one — this app carries a bearer token and a vault passphrase, and a release that tolerated
  http:// would send both in the clear the first time somebody typed an address without the s.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
